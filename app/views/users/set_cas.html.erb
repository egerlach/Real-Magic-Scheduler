<%= form_for(@user, :url => update_cas_user_path(@user)) do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.label :casid%><br />
    <%= f.text_field :casid%>
  </div>
  <div class="actions">
    <%= f.submit "Set CAS id"%>
  </div>
<% end %>
